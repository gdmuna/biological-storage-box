# Roadmap

本文件记录 **Biological Storage Box** 项目当前的功能缺口与后续规划。

> 状态说明：🔴 阻塞性缺口 · 🟠 核心工作流缺失 · 🟡 前端接入待完成 · 🟢 长期/进阶能力

---

## 现有功能快照

| 功能模块 | 数据模型 | 后端 API | 前端页面 |
|---------|---------|---------|---------|
| 认证（登录/注册/邮箱验证） | ✅ | ✅ | ✅ |
| 节点树（库室/储存盒层级） | ✅ Node | ✅ | ✅ |
| 试剂基本管理（增删改查） | ✅ Reagent | ✅ | ✅ |
| 试剂类型管理 | ✅ ReagentType | ✅ | ⚠️ 嵌于试剂页内 |
| 操作日志（试剂 / 节点） | ✅ ReagentLog / NodeLog | ✅ | ❌ 无独立页面 |
| 资源共享（跨组织） | ✅ ResourceShare | ✅ | ❌ 无前端页面 |
| 节点图可视化 | ✅ | ✅ | ✅ |
| 仪表盘概览 | — | — | ✅（基础统计） |

---

## 🔴 P0 — 数据模型层根本缺口

当前 `Reagent` 模型缺少生物样本管理系统必须具备的字段，**影响所有上层功能的实现**，需优先补齐。

| 字段 | 类型 | 说明 |
|------|------|------|
| `quantity` / `volume` | `Float?` | 库存数量或剩余体积；当前仅记录位置，无法追踪消耗 |
| `unit` | `String?` | 数量单位（mL、mg、U 等）；可与 ReagentType 共用，也可在试剂级别覆盖 |
| `expiryDate` | `DateTime?` | **有效期**，实验室安全合规的核心字段 |
| `manufactureDate` | `DateTime?` | 生产日期，配合有效期计算剩余有效时长 |
| `batchNo` | `String?` | **批次号**，用于溯源和投诉处理 |
| `catalogNo` | `String?` | 货号，对接采购和外部试剂数据库 |
| `manufacturer` | `String?` | 生产厂商 |
| `casNumber` | `String?` | CAS 号，化学试剂的全球唯一标识 |
| `storageCondition` | `Json?` | 储存条件（温度范围、避光等）；现有 `environment` 字段部分承担此职责，需结构化 |
| `hazardLevel` | `Enum?` | GHS 危险类别，影响储存分区策略 |
| `minStockThreshold` | `Float?` | 低库存预警阈值，低于此值时触发告警 |

**迁移范围**：新增 Schema 字段 + Prisma migration + 后端 DTO 更新 + 前端表单/列表同步扩展。

---

## 🟠 P1 — 核心工作流缺失

### 1. 节点图片信息存储

数据库层 `NodeImage` 模型已存在，文件上传基础设施（`FileService` / StorageService）已就绪，目前仅缺后端 API 与前端入口。

- [ ] 后端：新增 `POST /node/image/add`（接收 `nodeId` + `imageUrl`，写入 `NodeImage` 记录）
- [ ] 后端：新增 `DELETE /node/image/remove`（按 `nodeImageId` 删除）
- [ ] 后端：`NodeVo` 响应中携带 `images[]`（已有关联，只需 include）
- [ ] 前端：节点详情页（库室详情、储存盒详情）增加图片展示区
- [ ] 前端：上传入口：先调用 `FileService` presign → 客户端直传 S3 → 确认后调用 node image add 接口

### 2. 出库申请与审批流程

当前 `ReagentLog` 仅做事后记录（PLACED / TAKEN），缺少前置的"申请 → 审批 → 取用"流程。

- [ ] 新增 `ReagentRequest` 模型（申请人、申请数量、审批状态、审批人、审批时间）
- [ ] 后端：申请创建 / 审批通过 / 拒绝接口
- [ ] 前端：申请列表页、审批操作入口（在试剂详情或专属审批页面）

### 2. 库存预警与通知中心

- [ ] 后端：定时任务扫描 `expiryDate`（依赖 P0 字段）和 `quantity`，生成告警记录
- [ ] 新增 `Alert` / `Notification` 模型（类型、严重级别、已读状态）
- [ ] 前端：Dashboard 集成"即将过期试剂"和"低库存"快捷卡片
- [ ] 前端：通知中心页面或顶栏通知下拉

### 3. 采购申请 / 补货流程

- [ ] 新增 `ProcurementRequest` 模型（关联试剂类型、申请数量、申请人、状态、供应商）
- [ ] 前端：发起补货申请入口（从试剂详情或低库存告警跳转）
- [ ] 前端：采购申请列表与状态追踪页面

---

## 🟡 P2 — 前端接入待完成（后端已就绪）

### 0. 页面排版优化

库室列表、储存盒列表、试剂列表、试剂类型列表当前均采用大卡片布局，空白区域占比过高、信息密度不足。

- [ ] **库室列表**：改为紧凑表格行或小卡片网格，每行显示名称、子节点数、描述、操作
- [ ] **储存盒列表**：改为分组表格，每行显示名称、所属库室、网格规格、描述、操作
- [ ] **试剂列表**：改为紧凑行式列表（类 Notion 数据库视图），每行显示名称、位置、类型色标、库存、有效期、危险级别、操作
- [ ] **试剂类型列表**：改为紧凑表格，每行显示色块、名称、单位、描述、关联试剂数、操作

### 1. 资源共享管理页面

后端 `ResourceShare` 模型与 `share.ts` API 已完整实现，缺少前端页面。

- [ ] 共享申请发起（选择目标组织、权限级别）
- [ ] 收到的共享请求列表（接受 / 拒绝）
- [ ] 已授出共享列表（可撤销）
- 入口建议：移入"组织"管理页，或作为节点/库室详情的侧边面板

### 2. 操作记录查看器

`ReagentLog` 和 `NodeLog` 的后端查询接口已实现，前端无独立展示。

- [ ] 试剂详情页内嵌操作时间轴
- [ ] 全局操作日志页面（支持按操作类型、时间范围、操作人筛选）

### 3. 试剂类型独立管理页面

目前试剂类型嵌在试剂页内管理，不够直观。

- [ ] 独立的试剂类型列表页（颜色标签、单位、关联试剂数量统计）
- [ ] 类型编辑 / 删除 / 合并操作

---

## 🟢 P3 — 长期 / 进阶能力

### 全局搜索

- [ ] 跨节点、跨储存盒的试剂搜索
- [ ] 支持按 CAS 号、批次号、有效期范围等高级筛选

### 数据导出与盘库

- [ ] 当前库存清单导出（Excel / CSV）
- [ ] 按节点/组织范围导出
- [ ] 定期盘库报告生成

### 危险品与合规管理

- [ ] GHS 危险类别标注与储存区域匹配规则
- [ ] 安全数据表（SDS）文件关联与下载

### 批量操作

- [ ] 试剂批量导入（CSV 模板 + 字段映射）
- [ ] 批量移动（跨盒子/跨库室）
- [ ] 批量作废/删除

### 仪表盘增强

- [ ] 试剂类型分布饼图
- [ ] 近期操作活动流
- [ ] 库室空间占用率可视化

---

---

## 🔵 P2 — UI/UX 治理（源自 2026-05-15 审计，详见 [BSB-Frontend/docs/UI-UX-AUDIT.md](./BSB-Frontend/docs/UI-UX-AUDIT.md)）

> 以下条目来自对所有已实现页面的用户体验审计，按修复成本排序。

### FIX — 立即修复（可用性 / A11y）

- [ ] **FIX-01** 面包屑首项去除 GitHub 外链，改为跳转 `/dashboard`
- [ ] **FIX-02** 所有 `DialogContent` 补充 `<DialogDescription>`（修复 aria-describedby 警告）
- [ ] **FIX-03** 节点图图例英文翻译（ROOT→根节点，CONTAINER→库室，BOX→储存盒，BOX_SLOT→储位）
- [ ] **FIX-04** 操作日志 Tab 激活态改为 2px 底部下划线样式，去掉蓝色背景框
- [ ] **FIX-05** 试剂类型徽章颜色：使用 `color` 字段动态渲染，不再固定蓝色

### IMP — 高优改进（日常使用效率）

- [ ] **IMP-01** 侧边栏导航分组重构：拆分为"核心功能"、"库存管理"、"分析与记录"，将"组织"移出主导航
- [ ] **IMP-02** 建立统一空态组件 `EmptyState`（SVG 插图 + 说明 + CTA），应用于所有列表页
- [ ] **IMP-03** 试剂列表新增搜索栏 + 按试剂类型筛选 dropdown
- [ ] **IMP-04** 试剂列表每行整行可点击，跳转至试剂详情页
- [ ] **IMP-05** 试剂新建改为侧边抽屉（Sheet）或多步骤向导，解决 15+ 字段塞一个 Dialog 的问题
- [ ] **IMP-06** 操作日志默认展示全部日志，下拉改为筛选器（而非当前的"触发器"）
- [ ] **IMP-07** 库室列表行补充描述文本和子储存盒数统计
- [ ] **IMP-08** 储存盒列表行补充占用率统计（已用格数 / 总格数）

### DSY — 设计系统统一（品牌一致性）

- [ ] **DSY-01** `style.css` 补充 DESIGN.md 全量颜色 token（`card-tint-*`、`badge-*` 系列）
- [ ] **DSY-02** 建立徽章变体：`badge-success`、`badge-warning`、`badge-danger`、`badge-tag-*`
- [ ] **DSY-03** 修复全站排版层级（页面 `h1` 统一 `text-3xl`/`4xl`，引入 `font-display` 用于大标题）
- [ ] **DSY-04** 用 semantic 颜色替换日志操作类型和危险级颜色中散落的 Tailwind 随机色类
- [ ] **DSY-05** 仪表盘统计卡片使用 `card-tint-*` pastel 背景区分模块语义

### ENH — 体验增强（用户满意度）

- [ ] **ENH-01** 仪表盘添加"即将过期试剂"、"低库存预警"快捷卡片（依赖 P0 字段）
- [ ] **ENH-02** 仪表盘节点图加"展开全图"按钮跳转 `/node`
- [ ] **ENH-03** 操作日志改为时间轴（Timeline）视觉布局
- [ ] **ENH-04** 试剂危险级列添加 GHS 图形符号
- [ ] **ENH-05** 组织管理激活态改为绿色"当前组织"徽章
- [ ] **ENH-06** 储存盒格位网格悬停显示试剂名称 tooltip

---

## 导航优化待办

> 与功能无直接关系，但影响使用体验。

- [ ] **"组织"从主导航移出**：改为通过 `TeamSwitcher` 下拉菜单进入，减少与切换组织入口的职责重叠（见 IMP-01）
- [ ] **`/room` URI 评估**：如后端 API 同步重命名，可考虑更具领域语义的路径（如 `/storage-room`）
- [ ] **试剂类型管理入口**：提升为侧边栏独立条目，或在"试剂"页内以 Tab 形式分隔
